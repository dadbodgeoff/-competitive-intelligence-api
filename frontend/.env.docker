# Docker Environment
# Empty API URL means the frontend will call the same server it's served from
# This works because the backend serves both the frontend and API

VITE_API_URL=

# Disable analytics in Docker
VITE_SENTRY_DSN=
VITE_POSTHOG_KEY=
